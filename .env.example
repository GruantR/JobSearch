NODE_ENV=development
PORT=3000
SECRET_KEY=your_secret_key_for_jwt

# Лимит запросов к /api/auth/* (за 15 мин с одного IP), по умолчанию 50
# AUTH_RATE_LIMIT_MAX=50

# Первый администратор (после миграций создаётся/обновляется при старте)
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=change_me_min_6_chars

# Для локальной разработки:
DB_NAME=job_search
DB_USER=postgres
DB_PASS=password
DB_HOST=localhost
DB_PORT=5432

# Для продакшена:
# DATABASE_URL=postgresql://...


TELEGRAM_BOT_TOKEN= **************